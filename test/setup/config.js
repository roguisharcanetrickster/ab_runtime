/*
 * config.js
 * The user information our test runs will be made under.
 */

 module.exports = {
   tenant: "admin",
   // {string} tenant: The UUID of the tenant entry we are logging into

   user: {
      email: "[email]",
      // {string} email : the AuthLogin email login 

      password: "[password]",
      // {string} password: the AuthLogin password entry
   },
};
